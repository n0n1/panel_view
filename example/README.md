# panel_view_example

Example Flutter app for the `panel_view` package.

The app demonstrates managed floating panels with `PanelManagerService` and
`GlobalPanelOverlay`.

Run it with:

```sh
flutter run
```
