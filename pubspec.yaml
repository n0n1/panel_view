name: panel_view
description: A draggable and resizable floating panel system for Flutter apps.
version: 0.1.0
repository: https://github.com/n0n1/panel_view
issue_tracker: https://github.com/n0n1/panel_view/issues
topics:
  - flutter
  - widgets
  - panels

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
